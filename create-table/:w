function createTable(num) {
  const table = []
  const width = num + num - 1
  let itemCount = num

  for (let h = 0; h < num; h++) {
    let row = []

    for (let w = 0; w < width; w++){



      row.push(cell)
    }
    table.push(row)
    itemCount--
  }

  return table
}

console.log(createTable(5))
